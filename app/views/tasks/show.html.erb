<h1> Laundry </h1>
<%= @task.title  %>


<%= link_to 'back to your task', tasks_path %>
<%= link_to 'delete', delete_task_path(@task), data: { turbo_method: :delete, turbo_confirm: 'are u sure?' } %>
